<?php
/**
 * Small plugin-wide helpers. Add utilities here only when something
 * actually uses them, so this doesn't grow into a junk drawer.
 *
 * @package Timeslate
 */

declare( strict_types=1 );

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}
